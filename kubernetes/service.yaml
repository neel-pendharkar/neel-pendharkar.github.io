apiVersion: v1
kind: Service
metadata:
  name: personal-site
  labels:
    app.kubernetes.io/name: personal-site
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: personal-site
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30107
